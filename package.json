{
  "name": "agentgate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@worldcoin/agentkit": "^0.1.6",
    "dotenv": "^17.4.0",
    "ethers": "^6.16.0"
  }
}
